{
	"scripts": {
		"start": "run-p watch serve",
		"sass": "sass src/scss:src/css",
		"nosource": "npm run sass -- --no-source-map",
		"serve": "live-server --port=8001 src/",
		"watch": "npm run sass -- --watch"
	},
	"devDependencies": {
		"live-server": "^1.2.2",
		"npm-run-all": "^4.1.5",
		"sass": "^1.54.0"
	}
}
